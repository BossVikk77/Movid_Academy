---
layout: base-layout.njk
title: Pricing
permalink: /pricing/
---

<div class="container my-5">
  <h1 class="display-4 text-primary fw-bold mb-4 text-center"><i class="bi bi-tags-fill me-3"></i>Our Courses & Pricing</h1>
  <p class="lead text-center mb-5">Find the perfect plan to match your learning style and goals.</p>

  {% for course in courses %}
  <section class="mb-5">
    <h2 class="text-primary fw-bold border-bottom pb-2 mb-4 d-flex align-items-center">
      <img src="{{ course.logoUrl | url }}" 
           alt="{{ course.title }} Logo"
           width="32" 
           height="32"
           class="me-3"
           onerror="this.src='https://placehold.co/32x32'; this.onerror=null;">
      {{ course.title }}
    </h2>
    <p class="lead">{{ course.briefDescription }}</p>
    
    <div class="row row-cols-1 row-cols-md-{{ course.pricingTiers.length | min(3) }} g-4">
      {% for tier in course.pricingTiers %}
      <div classs="col">
        <div class="card h-100 shadow-sm">
          <div class="card-header py-3">
            <h4 class="my-0 fw-normal">{{ tier.name }}</h4>
          </div>
          <div class="card-body d-flex flex-column">
            <h2 class="card-title pricing-card-title">{{ tier.price }}</h2>
            <ul class="list-unstyled mt-3 mb-4 flex-grow-1">
              {% for feature in tier.features %}
                <li>{{ feature }}</li>
              {% endfor %}
            </ul>
            <a href="{{ '/courses/' + course.slug + '/' | url }}" class="btn btn-outline-primary mb-2">Learn More</a>
            <a href="{{ '/contact/' | url }}" class="btn btn-accent">Enroll Now</a>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </section>
  {% endfor %}
  
</div>